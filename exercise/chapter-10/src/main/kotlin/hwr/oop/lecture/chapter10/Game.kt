package hwr.oop.lecture.chapter10

// Aufgabe 4 (GameTest): Implementiere allCards(): List<Card>
//   Gibt alle Karten zurück, die sich auf den Händen aller Spieler befinden — als flache Liste.
class Game(val players: List<Player>) {
    // Aufgabe 4 — see GameTest
    fun allCards(): List<Card> {
        TODO("GameTest: collect all cards from all player hands into one list")
    }
}
